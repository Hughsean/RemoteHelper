# Release v1.0.0

## 🚀 新增功能 (New Features)

- **macOS 菜单栏常驻应用**:
  - 应用启动后默认隐藏 Dock 图标，仅在菜单栏显示托盘图标。
  - 点击菜单栏图标可显示/隐藏主窗口。
  - 启动时默认隐藏窗口，避免干扰用户。
- **系统托盘支持**:
  - 支持右键菜单：显示/隐藏、退出。
  - 左键点击托盘图标切换窗口显示状态。
- **窗口行为优化**:
  - 点击窗口关闭按钮 (X) 时，不再退出应用，而是隐藏窗口到后台。
  - Windows/Linux 平台启动时自动显示窗口，macOS 保持静默启动。
- **界面美化**:
  - 全新的毛玻璃 (Glassmorphism) 效果面板。
  - 列表加载时的交错动画效果。
  - 按钮和交互元素的过渡动画优化。
- **退出功能**:
  - 界面右上角新增“退出应用”按钮。
  - 托盘菜单新增“退出”选项。

## 🔒 安全 (Security)

- **客户端密钥加密**:
  - 新增客户端密钥生成与加密存储功能。
  - 增强了通信安全性。

## 🛠 构建与部署 (Build & CI)

- **自动化构建**:
  - 新增 GitHub Actions 工作流，自动构建 Windows (`.exe`/`.msi`) 与 macOS (`.dmg`) 安装包。
  - 支持 Apple Silicon (M1/M2/M3) 和 Intel 架构。

## 🐛 修复 (Fixes)

- 修复了应用启动时的白屏闪烁问题。
- 修复了列表刷新时的闪烁问题。
